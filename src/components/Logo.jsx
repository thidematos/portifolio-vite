function Logo({ fontSize }) {
  return (
    <h1
      className={`font-roboto tracking-tighter text-gray-800 ${fontSize} drop-shadow-md`}
    >
      Thiago L. Matos
    </h1>
  );
}

export default Logo;
